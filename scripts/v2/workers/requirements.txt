pg8000==1.31.5
boto3>=1.34
# AI Diagnosis report export (Fargate report job): DOCX via python-docx, markdown→HTML for the PDF,
# and headless chromium (playwright) for the high-fidelity PDF. chromium itself + Noto CJK fonts are
# installed in the Dockerfile (`playwright install --with-deps chromium`), not via pip.
python-docx==1.1.2
markdown==3.8.1
playwright==1.49.1
