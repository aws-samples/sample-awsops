---
sidebar_position: 2
---

import Screenshot from '@site/src/components/Screenshot';

# S3

Manage S3 (Simple Storage Service) buckets and monitor security status.

<Screenshot src="/screenshots/storage/s3.png" alt="S3" />

## Key Features

### Statistics Cards
- **Total Buckets**: Total number of buckets
- **Public Buckets**: Number of buckets with public access enabled (security warning)
- **Versioning**: Number of buckets with versioning enabled
- **Logging**: Number of buckets with access logging configured

### Bucket Map by Region
Buckets render as uniform block tiles grouped by region (instead of v1's area-proportional TreeMap):
- **Red**: Policy Public buckets (requires attention — bucket-policy scoped)
- **Green**: Buckets with versioning enabled
- **Cyan**: Standard buckets — green/cyan are bucket-policy-scoped too (ACL-based exposure is separate)
- **Gray**: Unknown status (policy/versioning flags unsynced or denied — never painted a confident color)

Click on a bucket block to navigate to the detail panel.

### Visualization Charts
- **Buckets by Region**: Bucket distribution by region
- **Security Status**: bucket counts per Policy Private/Policy Public/Versioned/Logging flag. The Policy bars measure **bucket-policy status only** (full exposure — e.g. BPA disabled — is the Security page's Public S3 check); a bucket with no policy counts as Policy Private, unknown (access-denied) buckets count into neither side, and the bars populate after the bucket-policy public flag is synced.

### Filtering
- Search box: Search by bucket name
- Region filter: View only specific regions
- Access filter: View only Public/Private buckets (the Policy Public facet — based on the synced bucket-policy public flag)

### Detail Panel
Information available when clicking on a bucket:
- Bucket name, region, ARN, creation date
- Security settings (Public Policy, Block ACLs, etc.)
- Versioning, encryption, lifecycle rules
- List of IAM roles with S3 access (**admin-only** — non-admins see a permission note; from the synced AWS managed policies AmazonS3*/AdministratorAccess/PowerUserAccess/ReadOnlyAccess [incl. job-function paths], max 30 — inline/bucket-policy-granted access not included; the last sync run's status gates conclusions — a failed run shows a stale-data banner and an empty result is only conclusive under a succeeded run within 24h on an untruncated (<500-row) page; a 'not synced' note shows before the policy lists sync)
- Tag information (shown after a terraform apply + bucket tags sync — no tags reads '—'; access-denied buckets show nothing)

## How to Use

### View Bucket List
1. Check the Bucket Map by Region (block tiles: Public=red > Versioned=green > Standard=cyan, unknown=gray) — clicking a block opens the detail panel
2. View detailed list in the table
3. Use filters to search for desired buckets

### Check Security Status
1. Check the number of public buckets in the Public Buckets card
2. Identify red buckets in the Bucket Map by Region
3. Select "Public" in the access filter to view the list

### Check IAM Permissions
The "IAM Roles with S3 Access" section of the bucket detail panel lists **account-wide roles holding broad S3 managed policies** (admin-only) — it is NOT a per-bucket access evaluation; every bucket detail shows the same list.

## Tips

:::tip Public Bucket Warning
Public buckets risk data exposure. Regularly check for unintentionally public buckets. If the Public Buckets card is displayed in red, immediate review is required.
:::

:::info Versioning Recommended
Enable versioning on buckets storing important data to prevent accidental deletion or overwriting. The number of buckets without versioning can be checked in the Versioning card.
:::

## AI Analysis Tips

Try asking the AI assistant:

- "Analyze the list of public S3 buckets and security risks"
- "Which buckets have versioning disabled?"
- "Analyze IAM roles with access to a specific bucket"
- "Check S3 bucket encryption status"

:::tip Data Gateway
The AI assistant supports S3 bucket analysis, security checks, and IAM policy simulation through the Data Gateway. In-depth security analysis is also possible in conjunction with the Security Gateway.
:::

## Related Pages

- [IAM](../security/iam) - Roles/users with S3 access permissions
- [Security](../security) - Public S3 bucket security check
- [CloudTrail](../monitoring/cloudtrail) - S3 API call history
